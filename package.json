{
  "name": "sillytavern-custom-plugin",
  "version": "0.3.1",
  "description": "Source-grounded precise and semantic story rewriting for SillyTavern messages.",
  "type": "module",
  "scripts": {
    "check": "node --check index.js && node --check lib/selection.js && node --check lib/rewrite.js && node --check lib/semantic.js && node --check lib/swipe.js && node --input-type=module -e \"await import('./index.js')\"",
    "test": "node --test"
  },
  "keywords": [
    "sillytavern",
    "extension",
    "rewrite",
    "story-editing"
  ],
  "license": "MIT"
}
